{
  "name": "tarea-1",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "rm -rf dist && vite build && git add dist && git commit -m 'Deploy' && git push"
  },
  "devDependencies": {
    "vite": "^6.2.0"
  },
  "dependencies": {
    "@effect-ts/core": "^0.60.5",
    "effect": "^3.14.1",
    "fp-ts": "^2.16.9",
    "lodash": "^4.17.21",
    "rxjs": "^7.8.2"
  }
}
